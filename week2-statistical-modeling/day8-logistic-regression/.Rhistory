library(tidyverse)
library(this.path)
library(pROC)
setwd(this.dir())
framingham <- read_csv("../../data/framingham.csv") |>
mutate(
male          = factor(male,          levels = c(0,1), labels = c("Female","Male")),
currentSmoker = factor(currentSmoker, levels = c(0,1), labels = c("Non-smoker","Smoker")),
prevalentHyp  = factor(prevalentHyp,  levels = c(0,1), labels = c("No","Yes")),
diabetes      = factor(diabetes,      levels = c(0,1), labels = c("No","Yes")),
TenYearCHD    = factor(TenYearCHD,    levels = c(0,1), labels = c("No","Yes")),
education     = factor(education,     levels = 1:4,
labels = c("Some HS","HS Diploma","Some College","College Degree"))
)
framingham |>
count(TenYearCHD) |>
mutate(percent = round(n / sum(n) * 100, 1))
#| fig-width: 7
#| fig-height: 4.5
#| message: false
framingham |>
mutate(CHD_numeric = as.numeric(TenYearCHD) - 1) |>
ggplot(aes(x = sysBP, y = CHD_numeric)) +
geom_jitter(alpha = 0.06, height = 0.04, color = "steelblue") +
geom_smooth(method = "loess", color = "firebrick", se = TRUE, linewidth = 1) +
labs(x = "Systolic BP (mmHg)",
y = "Probability of CHD within 10 years",
title = "Empirical CHD Probability by Systolic BP",
subtitle = "Loess smoother on the binary outcome") +
theme_minimal()
chd_simple <- glm(TenYearCHD ~ sysBP,
data = framingham,
family = binomial(link = "logit"))
summary(chd_simple)
exp(cbind(OR = coef(chd_simple), confint(chd_simple)))
exp(20 * coef(chd_simple)["sysBP"])
exp(20 * confint(chd_simple)["sysBP", ])
framingham <- framingham |>
mutate(p_chd = predict(chd_simple, type = "response"))
framingham |>
select(sysBP, TenYearCHD, p_chd) |>
slice_sample(n = 6)
new_subjects <- tibble(sysBP = c(110, 130, 150, 180))
new_subjects |>
mutate(p_chd = predict(chd_simple, newdata = new_subjects, type = "response"))
#| fig-width: 7
#| fig-height: 4.5
#| message: false
framingham |>
mutate(CHD_numeric = as.numeric(TenYearCHD) - 1) |>
ggplot(aes(x = sysBP, y = CHD_numeric)) +
geom_jitter(alpha = 0.06, height = 0.04, color = "steelblue") +
geom_smooth(method = "glm", method.args = list(family = "binomial"),
color = "firebrick", se = TRUE, linewidth = 1) +
labs(x = "Systolic BP (mmHg)",
y = "Probability of CHD within 10 years",
title = "Fitted Logistic Regression Curve",
subtitle = "Red curve: fitted logistic model; shaded band is 95% CI for the mean") +
theme_minimal()
framingham_bad <- framingham |>
mutate(sysBP_factor = cut(sysBP,
breaks = c(80, 120, 140, 160, 300),
labels = c("<120", "120-140", "140-160", "160+"),
include.lowest = TRUE))
bad_model <- glm(TenYearCHD ~ sysBP_factor,
data = framingham_bad,
family = binomial)
AIC(chd_simple, bad_model)
#| fig-width: 9
#| fig-height: 6
#| message: false
framingham |>
mutate(CHD_numeric = as.numeric(TenYearCHD) - 1) |>
pivot_longer(cols = c(age, sysBP, totChol, BMI),
names_to = "predictor", values_to = "value") |>
ggplot(aes(x = value, y = CHD_numeric)) +
geom_jitter(alpha = 0.05, height = 0.04, color = "steelblue") +
geom_smooth(method = "loess", color = "firebrick", se = TRUE, linewidth = 1) +
facet_wrap(~ predictor, scales = "free_x") +
labs(x = "Predictor value", y = "Probability of CHD",
title = "Empirical CHD Probability by Each Predictor") +
theme_minimal()
chd_full <- glm(TenYearCHD ~ age + currentSmoker + sysBP + totChol +
diabetes + BMI,
data = framingham,
family = binomial)
summary(chd_full)
exp(cbind(OR = coef(chd_full), confint(chd_full))) |>
round(3)
# Reduced model: drop diabetes and BMI
chd_reduced <- glm(TenYearCHD ~ age + currentSmoker + sysBP + totChol,
data = framingham,
family = binomial)
anova(chd_reduced, chd_full, test = "LRT")
# Reduced model: drop diabetes and BMI
chd_reduced <- glm(TenYearCHD ~ age + currentSmoker + sysBP + totChol,
data = framingham |> filter(!is.na(BMI)),
family = binomial)
anova(chd_reduced, chd_full, test = "LRT")
chd_step <- step(chd_full, direction = "both", trace = 0)
?step
# Chunk 1
library(tidyverse)
library(this.path)
library(pROC)
setwd(this.dir())
framingham <- read_csv("../../data/framingham.csv") |>
mutate(
male          = factor(male,          levels = c(0,1), labels = c("Female","Male")),
currentSmoker = factor(currentSmoker, levels = c(0,1), labels = c("Non-smoker","Smoker")),
prevalentHyp  = factor(prevalentHyp,  levels = c(0,1), labels = c("No","Yes")),
diabetes      = factor(diabetes,      levels = c(0,1), labels = c("No","Yes")),
TenYearCHD    = factor(TenYearCHD,    levels = c(0,1), labels = c("No","Yes")),
education     = factor(education,     levels = 1:4,
labels = c("Some HS","HS Diploma","Some College","College Degree"))
)
# Chunk 2
framingham |>
count(TenYearCHD) |>
mutate(percent = round(n / sum(n) * 100, 1))
# Chunk 3
#| fig-width: 7
#| fig-height: 4.5
#| message: false
framingham |>
mutate(CHD_numeric = as.numeric(TenYearCHD) - 1) |>
ggplot(aes(x = sysBP, y = CHD_numeric)) +
geom_jitter(alpha = 0.06, height = 0.04, color = "steelblue") +
geom_smooth(method = "loess", color = "firebrick", se = TRUE, linewidth = 1) +
labs(x = "Systolic BP (mmHg)",
y = "Probability of CHD within 10 years",
title = "Empirical CHD Probability by Systolic BP",
subtitle = "Loess smoother on the binary outcome") +
theme_minimal()
# Chunk 4
chd_simple <- glm(TenYearCHD ~ sysBP,
data = framingham,
family = binomial(link = "logit"))
summary(chd_simple)
# Chunk 5
exp(cbind(OR = coef(chd_simple), confint(chd_simple)))
# Chunk 6
exp(20 * coef(chd_simple)["sysBP"])
exp(20 * confint(chd_simple)["sysBP", ])
# Chunk 7
framingham <- framingham |>
mutate(p_chd = predict(chd_simple, type = "response"))
framingham |>
select(sysBP, TenYearCHD, p_chd) |>
slice_sample(n = 6)
# Chunk 8
new_subjects <- tibble(sysBP = c(110, 130, 150, 180))
new_subjects |>
mutate(p_chd = predict(chd_simple, newdata = new_subjects, type = "response"))
# Chunk 9
#| fig-width: 7
#| fig-height: 4.5
#| message: false
framingham |>
mutate(CHD_numeric = as.numeric(TenYearCHD) - 1) |>
ggplot(aes(x = sysBP, y = CHD_numeric)) +
geom_jitter(alpha = 0.06, height = 0.04, color = "steelblue") +
geom_smooth(method = "glm", method.args = list(family = "binomial"),
color = "firebrick", se = TRUE, linewidth = 1) +
labs(x = "Systolic BP (mmHg)",
y = "Probability of CHD within 10 years",
title = "Fitted Logistic Regression Curve",
subtitle = "Red curve: fitted logistic model; shaded band is 95% CI for the mean") +
theme_minimal()
# Chunk 10
framingham_bad <- framingham |>
mutate(sysBP_factor = cut(sysBP,
breaks = c(80, 120, 140, 160, 300),
labels = c("<120", "120-140", "140-160", "160+"),
include.lowest = TRUE))
bad_model <- glm(TenYearCHD ~ sysBP_factor,
data = framingham_bad,
family = binomial)
AIC(chd_simple, bad_model)
# Chunk 11
#| fig-width: 9
#| fig-height: 6
#| message: false
framingham |>
mutate(CHD_numeric = as.numeric(TenYearCHD) - 1) |>
pivot_longer(cols = c(age, sysBP, totChol, BMI),
names_to = "predictor", values_to = "value") |>
ggplot(aes(x = value, y = CHD_numeric)) +
geom_jitter(alpha = 0.05, height = 0.04, color = "steelblue") +
geom_smooth(method = "loess", color = "firebrick", se = TRUE, linewidth = 1) +
facet_wrap(~ predictor, scales = "free_x") +
labs(x = "Predictor value", y = "Probability of CHD",
title = "Empirical CHD Probability by Each Predictor") +
theme_minimal()
# Chunk 12
vars_used <- c("TenYearCHD", "age", "currentSmoker", "sysBP",
"totChol", "diabetes", "BMI")
framingham_cc <- framingham |>
drop_na(all_of(vars_used))
nrow(framingham) - nrow(framingham_cc)  # how many rows were dropped
# Chunk 13
chd_full <- glm(TenYearCHD ~ age + currentSmoker + sysBP + totChol +
diabetes + BMI,
data = framingham_cc,
family = binomial)
summary(chd_full)
# Chunk 14
exp(cbind(OR = coef(chd_full), confint(chd_full))) |>
round(3)
# Chunk 15
# Reduced model: drop diabetes and BMI
chd_reduced <- glm(TenYearCHD ~ age + currentSmoker + sysBP + totChol,
data = framingham_cc,
family = binomial)
anova(chd_reduced, chd_full, test = "LRT")
chd_final <- chd_step
chd_step <- step(chd_full, direction = "both", trace = 0)
summary(chd_step)
AIC(chd_full, chd_step)
chd_final <- chd_step
exp(cbind(OR = coef(chd_final), confint(chd_final))) |>
round(3)
framingham <- framingham |>
mutate(p_chd_final = predict(chd_final, type = "response"))
?predict.glm
framingham_cc <- framingham_cc |>
mutate(p_chd_final = predict(chd_final, type = "response"))
framingham_cc |>
select(TenYearCHD, p_chd_final) |>
summary()
m_smoker_quad <- glm(TenYearCHD ~ poly(currentSmoker,2),
data = framingham,
family = binomial)
# Chunk 1
library(tidyverse)
library(this.path)
setwd(this.dir())
framingham <- read_csv("../../data/framingham.csv") |>
mutate(
male          = factor(male,          levels = c(0,1), labels = c("Female","Male")),
currentSmoker = factor(currentSmoker, levels = c(0,1), labels = c("Non-smoker","Smoker")),
prevalentHyp  = factor(prevalentHyp,  levels = c(0,1), labels = c("No","Yes")),
diabetes      = factor(diabetes,      levels = c(0,1), labels = c("No","Yes")),
TenYearCHD    = factor(TenYearCHD,    levels = c(0,1), labels = c("No","Yes")),
education     = factor(education,     levels = 1:4,
labels = c("Some HS","HS Diploma","Some College","College Degree"))
)
# Chunk 2
framingham |>
count(currentSmoker, TenYearCHD) |>
group_by(currentSmoker) |>
mutate(prop = round(n / sum(n), 3)) |>
filter(TenYearCHD == "Yes")
# Chunk 3
m_smoker <- glm(TenYearCHD ~ currentSmoker,
data = framingham,
family = binomial)
summary(m_smoker)
# Chunk 4
exp(cbind(OR = coef(m_smoker), confint(m_smoker))) |> round(3)
m_smoker_quad <- glm(TenYearCHD ~ poly(currentSmoker,2),
data = framingham,
family = binomial)
# Chunk 1
library(tidyverse)
library(this.path)
setwd(this.dir())
framingham <- read_csv("../../data/framingham.csv") |>
mutate(
male          = factor(male,          levels = c(0,1), labels = c("Female","Male")),
currentSmoker = factor(currentSmoker, levels = c(0,1), labels = c("Non-smoker","Smoker")),
prevalentHyp  = factor(prevalentHyp,  levels = c(0,1), labels = c("No","Yes")),
diabetes      = factor(diabetes,      levels = c(0,1), labels = c("No","Yes")),
TenYearCHD    = factor(TenYearCHD,    levels = c(0,1), labels = c("No","Yes")),
education     = factor(education,     levels = 1:4,
labels = c("Some HS","HS Diploma","Some College","College Degree"))
)
# Chunk 2
framingham |>
count(currentSmoker, TenYearCHD) |>
group_by(currentSmoker) |>
mutate(prop = round(n / sum(n), 3)) |>
filter(TenYearCHD == "Yes")
# Chunk 3
m_smoker <- glm(TenYearCHD ~ currentSmoker,
data = framingham,
family = binomial)
summary(m_smoker)
# Chunk 4
exp(cbind(OR = coef(m_smoker), confint(m_smoker))) |> round(3)
# Chunk 5
tab <- table(framingham$currentSmoker, framingham$TenYearCHD)
tab
or_table <- (tab[2,2] * tab[1,1]) / (tab[1,2] * tab[2,1])
round(or_table, 3)
# Chunk 6
#| fig-width: 7
#| fig-height: 4.5
#| message: false
framingham |>
mutate(CHD_numeric = as.numeric(TenYearCHD) - 1) |>
ggplot(aes(x = sysBP, y = CHD_numeric)) +
geom_jitter(alpha = 0.06, height = 0.04, color = "steelblue") +
geom_smooth(method = "loess", color = "firebrick", se = TRUE, linewidth = 1) +
labs(x = "Systolic BP (mmHg)",
y = "Probability of CHD within 10 years",
title = "Empirical CHD Probability by Systolic BP") +
theme_minimal()
# Chunk 7
m_bp <- glm(TenYearCHD ~ sysBP,
data = framingham,
family = binomial)
summary(m_bp)
exp(cbind(OR = coef(m_bp), confint(m_bp))) |> round(4)
m_smoker_quad <- glm(TenYearCHD ~ poly(currentSmoker,2),
data = framingham,
family = binomial)
m_smoker_quad <- glm(TenYearCHD ~ poly(sysBP,2),
data = framingham,
family = binomial)
summary(m_smoker_quad)
anova(m_smoker,m_smoker_quad)
m_bp_quad <- glm(TenYearCHD ~ poly(sysBP,2),
data = framingham,
family = binomial)
summary(m_bp_quad)
anova(m_bp,m_bp_quad)
# Chunk 1
library(tidyverse)
library(this.path)
setwd(this.dir())
framingham <- read_csv("../../data/framingham.csv") |>
mutate(
male          = factor(male,          levels = c(0,1), labels = c("Female","Male")),
currentSmoker = factor(currentSmoker, levels = c(0,1), labels = c("Non-smoker","Smoker")),
prevalentHyp  = factor(prevalentHyp,  levels = c(0,1), labels = c("No","Yes")),
diabetes      = factor(diabetes,      levels = c(0,1), labels = c("No","Yes")),
TenYearCHD    = factor(TenYearCHD,    levels = c(0,1), labels = c("No","Yes")),
education     = factor(education,     levels = 1:4,
labels = c("Some HS","HS Diploma","Some College","College Degree"))
)
# Chunk 2
framingham |>
count(currentSmoker, TenYearCHD) |>
group_by(currentSmoker) |>
mutate(prop = round(n / sum(n), 3)) |>
filter(TenYearCHD == "Yes")
# Chunk 3
m_smoker <- glm(TenYearCHD ~ currentSmoker,
data = framingham,
family = binomial)
summary(m_smoker)
# Chunk 4
exp(cbind(OR = coef(m_smoker), confint(m_smoker))) |> round(3)
# Chunk 5
tab <- table(framingham$currentSmoker, framingham$TenYearCHD)
tab
or_table <- (tab[2,2] * tab[1,1]) / (tab[1,2] * tab[2,1])
round(or_table, 3)
# Chunk 6
#| fig-width: 7
#| fig-height: 4.5
#| message: false
framingham |>
mutate(CHD_numeric = as.numeric(TenYearCHD) - 1) |>
ggplot(aes(x = sysBP, y = CHD_numeric)) +
geom_jitter(alpha = 0.06, height = 0.04, color = "steelblue") +
geom_smooth(method = "loess", color = "firebrick", se = TRUE, linewidth = 1) +
labs(x = "Systolic BP (mmHg)",
y = "Probability of CHD within 10 years",
title = "Empirical CHD Probability by Systolic BP") +
theme_minimal()
# Chunk 7
m_bp <- glm(TenYearCHD ~ sysBP,
data = framingham,
family = binomial)
summary(m_bp)
exp(cbind(OR = coef(m_bp), confint(m_bp))) |> round(4)
# Chunk 8
m_bp_quad <- glm(TenYearCHD ~ poly(sysBP,2),
data = framingham,
family = binomial)
summary(m_bp_quad)
anova(m_bp,m_bp_quad)
# Chunk 9
exp(20 * coef(m_bp)["sysBP"])
exp(20 * confint(m_bp)["sysBP", ])
# Chunk 10
#| fig-width: 7
#| fig-height: 4.5
#| message: false
framingham |>
mutate(CHD_numeric = as.numeric(TenYearCHD) - 1) |>
ggplot(aes(x = sysBP, y = CHD_numeric)) +
geom_jitter(alpha = 0.06, height = 0.04, color = "steelblue") +
geom_smooth(method = "glm", method.args = list(family = "binomial"),
color = "firebrick", se = TRUE, linewidth = 1) +
labs(x = "Systolic BP (mmHg)",
y = "Probability of CHD within 10 years",
title = "Fitted Logistic Regression: CHD on Systolic BP") +
theme_minimal()
# Chunk 11
m_smoke_only <- glm(TenYearCHD ~ currentSmoker,
data = framingham,
family = binomial)
exp(cbind(OR = coef(m_smoke_only), confint(m_smoke_only))) |> round(3)
# Chunk 12
m_smoke_age <- glm(TenYearCHD ~ currentSmoker + age,
data = framingham,
family = binomial)
exp(cbind(OR = coef(m_smoke_age), confint(m_smoke_age))) |> round(3)
# Chunk 13
framingham |>
group_by(currentSmoker) |>
summarize(mean_age = mean(age), .groups = "drop")
anova(m_smoke_only, m_smoke_age)
vars_used <- c("TenYearCHD", "age", "male", "currentSmoker",
"sysBP", "totChol", "BMI", "diabetes", "prevalentHyp")
framingham_cc <- framingham |>
drop_na(all_of(vars_used))
nrow(framingham) - nrow(framingham_cc)
m_full <- glm(TenYearCHD ~ age + male + currentSmoker + sysBP +
totChol + BMI + diabetes + prevalentHyp,
data = framingham_cc,
family = binomial)
summary(m_full)
m_step <- step(m_full, direction = "both", trace = 0)
summary(m_step)
AIC(m_full, m_step)
anova(m_step, m_full, test = "LRT")
chd_final <- m_step
or_table <- exp(cbind(OR = coef(chd_final), confint(chd_final))) |>
round(3)
or_table
exp(20 * coef(chd_final)["sysBP"])
exp(20 * confint(chd_final)["sysBP", ])
exp(40 * coef(chd_final)["totChol"])
exp(40 * confint(chd_final)["totChol", ])
exp(100 * coef(chd_final)["totChol"])
exp(100 * confint(chd_final)["totChol", ])
framingham_cc <- framingham_cc |>
mutate(p_final = predict(chd_final, newdata = framingham_cc,
type = "response"))
framingham_cc |>
select(TenYearCHD, p_final) |>
summary()
framingham_cc |>
mutate(CHD_numeric = as.numeric(TenYearCHD) - 1) |>
ggplot(aes(x = p_final, fill = TenYearCHD)) +
geom_histogram(binwidth = 0.02, position = "identity",
alpha = 0.6, color = "white") +
scale_fill_manual(values = c("No" = "steelblue", "Yes" = "firebrick")) +
labs(x = "Predicted probability of CHD",
y = "Count",
fill = "Actual CHD",
title = "Distribution of Predicted Probabilities by Actual Outcome") +
theme_minimal()

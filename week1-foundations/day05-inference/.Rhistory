# Observed SD of the simulated sample means
sd(means_n5)
sd(means_n30)
sd(means_n100)
# Chunk 12
# Always look first
framingham |>
ggplot(aes(x = sysBP)) +
geom_histogram(binwidth = 5, fill = "steelblue", color = "white") +
geom_vline(xintercept = 122, linetype = "dashed", color = "firebrick",
linewidth = 1) +
annotate("text", x = 125, y = 250, label = "US average (122 mmHg)",
color = "firebrick", hjust = 0, size = 3.5) +
labs(x = "Systolic BP (mmHg)", y = "Count",
title = "Systolic BP in the Framingham Cohort") +
theme_bw()
# Chunk 13
t.test(framingham$sysBP, mu = 122)
# Chunk 1
#| echo: false
library(tidyverse)
library(this.path)
setwd(this.dir())
framingham <- read_csv("../../data/framingham.csv") |>
mutate(
male            = factor(male,            levels = c(0, 1),
labels = c("Female", "Male")),
currentSmoker   = factor(currentSmoker,   levels = c(0, 1),
labels = c("Non-smoker", "Smoker")),
BPMeds          = factor(BPMeds,          levels = c(0, 1),
labels = c("No", "Yes")),
prevalentStroke = factor(prevalentStroke, levels = c(0, 1),
labels = c("No", "Yes")),
prevalentHyp    = factor(prevalentHyp,    levels = c(0, 1),
labels = c("No", "Yes")),
diabetes        = factor(diabetes,        levels = c(0, 1),
labels = c("No", "Yes")),
TenYearCHD      = factor(TenYearCHD,      levels = c(0, 1),
labels = c("No", "Yes")),
education       = factor(education,       levels = 1:4,
labels = c("Some HS", "HS Diploma",
"Some College",
"College Degree"))
)
# Chunk 3
# Draw 1000 values from N(mean = 0.8, sd = 0.3) -- the crab weight example
set.seed(42)
crab_weights <- rnorm(n = 1000, mean = 0.8, sd = 0.3)
# What fraction weigh more than 1 kg?
mean(crab_weights > 1)
# The exact probability from the distribution
1 - pnorm(1, mean = 0.8, sd = 0.3)
# Chunk 4
# Visualize
tibble(weight = crab_weights) |>
ggplot(aes(x = weight)) +
geom_histogram(aes(y = after_stat(density)), binwidth = 0.05,
fill = "steelblue", color = "white") +
stat_function(fun = dnorm, args = list(mean = 0.8, sd = 0.3),
color = "firebrick", linewidth = 1) +
geom_vline(xintercept = 1, linetype = "dashed", color = "gray40") +
annotate("text", x = 1.05, y = 1.1, label = "1 kg threshold",
hjust = 0, color = "gray40", size = 3.5) +
labs(x = "Weight (kg)", y = "Density",
title = "Simulated Blue Crab Weights",
subtitle = "N(0.8, 0.3²) — red curve is the true density") +
theme_bw()
# Chunk 5
mu <- 0.8; sigma <- 0.3
# Probability within 1, 2, and 3 standard deviations
pnorm(mu + sigma, mu, sigma) - pnorm(mu - sigma, mu, sigma)
pnorm(mu + 2*sigma, mu, sigma) - pnorm(mu - 2*sigma, mu, sigma)
pnorm(mu + 3*sigma, mu, sigma) - pnorm(mu - 3*sigma, mu, sigma)
# Chunk 6
# 20 patients, each with 30% response probability
# How many respond?
set.seed(42)
rbinom(n = 1, size = 20, prob = 0.3)   # one experiment
rbinom(n = 10, size = 20, prob = 0.3)  # ten experiments
# Simulate 10,000 experiments and look at the distribution
sim_responses <- rbinom(n = 10000, size = 20, prob = 0.3)
tibble(responses = sim_responses) |>
ggplot(aes(x = responses)) +
geom_bar(fill = "steelblue") +
labs(x = "Number of responders (out of 20)",
y = "Count (out of 10,000 simulations)",
title = "Binomial(n = 20, p = 0.30)") +
theme_bw()
# Chunk 7
# Exact probability that exactly 8 out of 20 respond
dbinom(8, size = 20, prob = 0.3)
# Probability that 10 or more respond
1 - pbinom(9, size = 20, prob = 0.3)
# Chunk 8
# Mutations per kilobase, average rate lambda = 2
sim_mutations <- rpois(n = 10000, lambda = 2)
tibble(mutations = sim_mutations) |>
ggplot(aes(x = mutations)) +
geom_bar(fill = "steelblue") +
labs(x = "Mutations per kilobase",
y = "Count",
title = "Poisson(λ = 2)") +
theme_bw()
# Probability of 0 mutations in a kilobase
dpois(0, lambda = 2)
# Probability of 5 or more
1 - ppois(4, lambda = 2)
# Chunk 9
# Simulate from an exponential distribution -- strongly right-skewed
set.seed(123)
population <- rexp(100000, rate = 0.5)  # mean = 1/rate = 2
tibble(x = population) |>
ggplot(aes(x = x)) +
geom_histogram(binwidth = 0.3, fill = "steelblue", color = "white") +
labs(x = "Value", y = "Count",
title = "The population distribution (Exponential)",
subtitle = "Strongly right-skewed — far from Normal") +
theme_bw()
# Chunk 10
# Function: draw B samples of size n, compute their means
sample_means <- function(population, n, B = 5000) {
replicate(B, mean(sample(population, size = n)))
}
set.seed(123)
means_n5   <- sample_means(population, n = 5)
means_n30  <- sample_means(population, n = 30)
means_n100 <- sample_means(population, n = 100)
# Stack them for plotting
clt_data <- bind_rows(
tibble(xbar = means_n5,   n = "n = 5"),
tibble(xbar = means_n30,  n = "n = 30"),
tibble(xbar = means_n100, n = "n = 100")
) |>
mutate(n = factor(n, levels = c("n = 5", "n = 30", "n = 100")))
ggplot(clt_data, aes(x = xbar)) +
geom_histogram(aes(y = after_stat(density)), bins = 40,
fill = "steelblue", color = "white") +
facet_wrap(~ n, scales = "free") +
labs(x = "Sample mean", y = "Density",
title = "Sampling Distribution of the Mean",
subtitle = "Population is exponential (skewed). As n grows, the mean becomes Normal.") +
theme_bw()
# Chunk 11
pop_sd <- sd(population)
# Theoretical SE = sigma / sqrt(n)
pop_sd / sqrt(5)
pop_sd / sqrt(30)
pop_sd / sqrt(100)
# Observed SD of the simulated sample means
sd(means_n5)
sd(means_n30)
sd(means_n100)
# Chunk 12
# Always look first
framingham |>
ggplot(aes(x = sysBP)) +
geom_histogram(binwidth = 5, fill = "steelblue", color = "white") +
geom_vline(xintercept = 122, linetype = "dashed", color = "firebrick",
linewidth = 1) +
annotate("text", x = 125, y = 250, label = "US average (122 mmHg)",
color = "firebrick", hjust = 0, size = 3.5) +
labs(x = "Systolic BP (mmHg)", y = "Count",
title = "Systolic BP in the Framingham Cohort") +
theme_bw()
# Chunk 13
t.test(framingham$sysBP, mu = 122)
framingham |>
ggplot(aes(x = male, y = sysBP, fill = male)) +
geom_boxplot(alpha = 0.7, show.legend = FALSE) +
labs(x = "Sex", y = "Systolic BP (mmHg)",
title = "Systolic BP by Sex") +
theme_bw()
# Group-level summaries first
framingham |>
group_by(male) |>
summarize(
n        = n(),
mean_bp  = mean(sysBP, na.rm = TRUE),
sd_bp    = sd(sysBP, na.rm = TRUE)
)
# Welch two-sample t-test (default -- does not assume equal variances)
t.test(sysBP ~ male, data = framingham)
framingham
framingham |>
ggplot(aes(x = male, y = totChol, fill = male)) +
geom_boxplot(alpha = 0.7, show.legend = FALSE) +
labs(x = "Sex", y = "Systolic BP (mmHg)",
title = "Systolic BP by Sex") +
theme_bw()
# Group-level summaries first
framingham |>
group_by(male) |>
summarize(
n        = n(),
mean_bp  = mean(totChol, na.rm = TRUE),
sd_bp    = sd(totChol, na.rm = TRUE)
)
# Welch two-sample t-test (default -- does not assume equal variances)
t.test(totChol ~ male, data = framingham)
# Group-level summaries first
chol_by_sex <- framingham |>
group_by(male) |>
summarize(
n        = n(),
mean_bp  = mean(totChol, na.rm = TRUE),
sd_bp    = sd(totChol, na.rm = TRUE)
)
chol_by_sex
chol_by_sex$mean_bp[1] - chol_by_sex$mean_bp[2]
# Welch two-sample t-test (default -- does not assume equal variances)
t.test(totChol ~ male, data = framingham)
# Group-level summaries first
chol_by_sex <- framingham |>
group_by(male) |>
summarize(
n        = n(),
mean_bp  = mean(totChol, na.rm = TRUE),
sd_bp    = sd(totChol, na.rm = TRUE)
)
chol_by_sex
# Welch two-sample t-test (default -- does not assume equal variances)
t.test(totChol ~ male, data = framingham)
chol_by_sex$mean_bp[1] - chol_by_sex$mean_bp[2]
# Chunk 1
library(tidyverse)
library(this.path)
this.dir <- this.dir()
setwd(this.dir)
framingham <- read_csv("../../data/framingham.csv") |>
mutate(
male            = factor(male,            levels = c(0, 1),
labels = c("Female", "Male")),
currentSmoker   = factor(currentSmoker,   levels = c(0, 1),
labels = c("Non-smoker", "Smoker")),
BPMeds          = factor(BPMeds,          levels = c(0, 1),
labels = c("No", "Yes")),
prevalentStroke = factor(prevalentStroke, levels = c(0, 1),
labels = c("No", "Yes")),
prevalentHyp    = factor(prevalentHyp,    levels = c(0, 1),
labels = c("No", "Yes")),
diabetes        = factor(diabetes,        levels = c(0, 1),
labels = c("No", "Yes")),
TenYearCHD      = factor(TenYearCHD,      levels = c(0, 1),
labels = c("No", "Yes")),
education       = factor(education,       levels = 1:4,
labels = c("Some HS", "HS Diploma",
"Some College",
"College Degree"))
)
framingham
ggplot(framingham, aes(y = diaBP)) +
geom_histogram(binwidth = 2) +
geom_vline(xintercept = 80, linetype = "dashed",
color = "gray40", linewidth = 0.8) +
annotate("text", x = 32, y = 82,
label = "80 mmHg",
hjust = 0, size = 3, color = "gray40") +
theme_minimal() +
labs(
title    = "Histogram of Diastolic Blood Pressure",
subtitle = "Framingham Heart Study — linear trends per group",
caption  = "Dashed line indicates at 80 mmHg)",
x        = "Diastolic Blood Pressure (mmHg)"
)
ggplot(framingham, aes(x = diaBP)) +
geom_histogram(binwidth = 2) +
geom_vline(xintercept = 80, linetype = "dashed",
color = "gray40", linewidth = 0.8) +
annotate("text", x = 32, y = 82,
label = "80 mmHg",
hjust = 0, size = 3, color = "gray40") +
theme_minimal() +
labs(
title    = "Histogram of Diastolic Blood Pressure",
subtitle = "Framingham Heart Study — linear trends per group",
caption  = "Dashed line indicates at 80 mmHg)",
x        = "Diastolic Blood Pressure (mmHg)"
)
ggplot(framingham, aes(x = diaBP)) +
geom_histogram(binwidth = 3) +
geom_vline(xintercept = 80, linetype = "dashed",
color = "gray40", linewidth = 0.8) +
annotate("text", x = 82, y = 400,
label = "80 mmHg",
hjust = 0, size = 3, color = "Red") +
theme_minimal() +
labs(
title    = "Histogram of Diastolic Blood Pressure",
subtitle = "Framingham Heart Study — linear trends per group",
caption  = "Dashed line indicates at 80 mmHg)",
x        = "Diastolic Blood Pressure (mmHg)"
)
ggplot(framingham, aes(x = diaBP)) +
geom_histogram(binwidth = 3) +
geom_vline(xintercept = 80, linetype = "dashed",
color = "red", linewidth = 0.8) +
annotate("text", x = 82, y = 400,
label = "80 mmHg",
hjust = 0, size = 3, color = "red") +
theme_minimal() +
labs(
title    = "Histogram of Diastolic Blood Pressure",
subtitle = "Framingham Heart Study — linear trends per group",
caption  = "Dashed line indicates at 80 mmHg)",
x        = "Diastolic Blood Pressure (mmHg)"
)
ggplot(framingham, aes(x = diaBP)) +
geom_histogram(binwidth = 3) +
geom_vline(xintercept = 80, linetype = "dashed",
color = "red", linewidth = 1.5) +
annotate("text", x = 85, y = 500,
label = "80 mmHg",
hjust = 0, size = 5, color = "red") +
theme_minimal() +
labs(
title    = "Histogram of Diastolic Blood Pressure",
subtitle = "Framingham Heart Study — linear trends per group",
caption  = "Dashed line indicates at 80 mmHg)",
x        = "Diastolic Blood Pressure (mmHg)"
)
t.test(diaBP, data = framingham, mu = 80)
t.test(framingham$diaBP, mu = 80)
# Always look at the raw counts first
tab <- table(Smoker = framingham$currentSmoker,
Hypertension = framingham$prevalentHyp)
tab
chisq.test(tab)$expected
result_chisq <- chisq.test(tab)
result_chisq
# Always look at the raw counts first
tab <- table(Smoker = framingham$currentSmoker,
Hypertension = framingham$prevalentHyp)
tab
# Get the proportions for smokers and non-smokers.
prop_values <- framingham |>
filter(!is.na(currentSmoker), !is.na(prevalentHyp)) |>
group_by(currentSmoker) |>
summarise(
prop_hyper = mean(prevalentHyp == "Yes")
)
prop_values
prop_values[2] - prop_values[1]
# Get the proportions for smokers and non-smokers.
prop_values <- framingham |>
filter(!is.na(currentSmoker), !is.na(prevalentHyp)) |>
group_by(currentSmoker) |>
summarise(
prop_hyper = mean(prevalentHyp == "Yes")
)
prop_values
round(prop_values$prop_hyper[2] - prop_values$prop_hyper[1], 3)
round(prop_values$prop_hyper[1] - prop_values$prop_hyper[2], 4)
framingham |>
filter(!is.na(currentSmoker), !is.na(prevalentHyp)) |>
group_by(currentSmoker, prevalentHyp) |>
summarize(n = n(), .groups = "drop") |>
group_by(currentSmoker) |>
mutate(pct = n / sum(n) * 100) |>
ggplot(aes(x = currentSmoker, y = pct, fill = prevalentHyp)) +
geom_col(position = "dodge") +
scale_fill_brewer(palette = "Set2") +
labs(x = "Smoking Status", y = "Percent (%)",
fill = "Hypertension",
title = "Hypertension Prevalence by Smoking Status") +
theme_bw()
framingham |>
ggplot(aes(x = diabetes, y = BMI, fill = diabetes)) +
geom_boxplot(alpha = 0.7, show.legend = FALSE) +
labs(x = "Diabetes Status", y = "Body Mass Index",
title = "Body Mass Index by Diabetes Status") +
theme_bw()
labs(y = "Body Mass Index",
title = "Body Mass Index by Diabetes Status") +
theme_bw()
framingham |>
ggplot(aes(y = BMI)) +
geom_histogram(alpha = 0.7, show.legend = FALSE) +
facet_wrap(~diabetes) +
labs(y = "Body Mass Index",
title = "Body Mass Index by Diabetes Status") +
theme_bw()
framingham |>
ggplot(aes(x = BMI)) +
geom_histogram(alpha = 0.7, show.legend = FALSE) +
facet_wrap(~diabetes) +
labs(y = "Body Mass Index",
title = "Body Mass Index by Diabetes Status") +
theme_bw()
framingham |>
ggplot(aes(x = BMI)) +
geom_histogram(alpha = 0.7, show.legend = FALSE) +
facet_wrap(~diabetes, scales = "free") +
labs(y = "Body Mass Index",
title = "Body Mass Index by Diabetes Status") +
theme_bw()
framingham |>
ggplot(aes(x = BMI, fill = diabetes)) +
geom_histogram(alpha = 0.7, show.legend = FALSE) +
facet_wrap(~diabetes, scales = "free") +
labs(y = "Body Mass Index",
title = "Body Mass Index by Diabetes Status") +
theme_bw()
# Group-level summaries first
framingham |>
group_by(diabetes) |>
summarize(
n        = n(),
mean_bp  = mean(BMI, na.rm = TRUE),
sd_bp    = sd(BMI, na.rm = TRUE)
)
# Welch two-sample t-test (default -- does not assume equal variances)
t.test(BMI ~ diabetes, data = framingham)
wilcox.test(BMI ~ diabetes, data = framingham,
conf.int = TRUE)   # request the Hodges-Lehmann CI
framingham |>
ggplot(aes(x = glucose, fill = diabetes)) +
geom_histogram(alpha = 0.7, show.legend = FALSE) +
facet_wrap(~diabetes, scales = "free") +
labs(y = "Glucose",
title = "Glucose by Diabetes Status") +
theme_bw()
# Group-level summaries first
framingham |>
group_by(diabetes) |>
summarize(
n        = n(),
mean_bp  = mean(glucose, na.rm = TRUE),
sd_bp    = sd(glucose, na.rm = TRUE)
)
?t.test
# Welch two-sample t-test
t.test(glucose ~ diabetes, data = framingham)
wilcox.test(glucose ~ diabetes, data = framingham,
conf.int = TRUE)   # request the Hodges-Lehmann CI
# Group-level summaries first
framingham |>
group_by(diabetes) |>
summarize(
n        = n(),
mean_bp  = mean(glucose, na.rm = TRUE),
sd_bp    = sd(glucose, na.rm = TRUE)
)
# Group-level summaries first
framingham |>
group_by(diabetes) |>
summarize(
n        = n(),
mean_glu  = mean(glucose, na.rm = TRUE),
sd_glu    = sd(glucose, na.rm = TRUE)
)
# Always look at the raw counts first
tab <- table(Education = framingham$education,
Smoking = framingham$currentSmoker)
tab
# Proportions within each education group (row proportions)
prop.table(tab, margin = 1) |> round(3)
# Visualize before testing
framingham |>
filter(!is.na(currentSmoker), !is.na(education)) |>
group_by(currentSmoker, education) |>
summarize(n = n(), .groups = "drop") |>
group_by(currentSmoker) |>
mutate(pct = n / sum(n) * 100) |>
ggplot(aes(x = education, y = pct, fill = prevalentHyp)) +
geom_col(position = "dodge") +
scale_fill_brewer(palette = "Set2") +
labs(x = "Education", y = "Percent (%)",
fill = "Hypertension",
title = "Hypertension Prevalence by Education Level") +
theme_bw()
# Visualize before testing
framingham |>
filter(!is.na(currentSmoker), !is.na(education)) |>
group_by(currentSmoker, education) |>
summarize(n = n(), .groups = "drop") |>
group_by(currentSmoker) |>
mutate(pct = n / sum(n) * 100) |>
ggplot(aes(x = education, y = pct, fill = currentSmoker)) +
geom_col(position = "dodge") +
scale_fill_brewer(palette = "Set2") +
labs(x = "Education", y = "Percent (%)",
fill = "Smoking Status",
title = "Smoking Status by Education Level") +
theme_bw()
result_chisq <- chisq.test(tab)
result_chisq
# Always check expected counts -- all should be >= 5
result_chisq$expected
framingham |>
filter(!is.na(currentSmoker), !is.na(education)) |>
group_by(currentSmoker, education) |>
summarize(n = n(), .groups = "drop")
framingham |>
filter(!is.na(currentSmoker), !is.na(education)) |>
group_by(currentSmoker, education) |>
summarize(n = n(), .groups = "drop") |>
group_by(education) |>
mutate(pct = n / sum(n) * 100)
# Visualize before testing
framingham |>
filter(!is.na(currentSmoker), !is.na(education)) |>
group_by(currentSmoker, education) |>
summarize(n = n(), .groups = "drop") |>
group_by(education) |>
mutate(pct = n / sum(n) * 100) |>
ggplot(aes(x = education, y = pct, fill = currentSmoker)) +
geom_col(position = "dodge") +
scale_fill_brewer(palette = "Set2") +
labs(x = "Education", y = "Percent (%)",
fill = "Smoking Status",
title = "Smoking Status by Education Level") +
theme_bw()

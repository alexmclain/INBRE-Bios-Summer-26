) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
ggplot(df_long, aes(x = Method, y = MSPE, fill = Method)) +
geom_boxplot(
width = 0.6,
alpha = 0.7,
outlier.shape = NA,
median.linewidth = 0   # removes median line
) +
stat_summary(
fun = mean,
geom = "crossbar",
width = 0.8,
color = "black",
linewidth = 1
) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
ggplot(df_long, aes(x = Method, y = MSPE, fill = Method)) +
geom_boxplot(
width = 0.6,
alpha = 0.7,
outlier.shape = NA,
median.linewidth = 0   # removes median line
) +
stat_summary(
fun = mean,
geom = "crossbar",
width = 0.7,
color = "black",
linewidth = 1
) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
ggplot(df_long, aes(x = Method, y = MSPE, fill = Method)) +
geom_boxplot(
width = 0.6,
alpha = 0.7,
outlier.shape = NA,
median.linewidth = 0   # removes median line
) +
stat_summary(
fun = mean,
geom = "crossbar",
width = 0.6,
color = "black",
linewidth = 1
) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
method_colors <- c(
"spexvb" = "firebrick1",
"hspvb" = "chartreuse4",
"sparsevb_Laplace" = "cornflowerblue",
"sparsevb_Gaussian" = "lightsteelblue4",
"varbvs" = "darkgoldenrod4",
"pxvb" = "coral"
)
ggplot(df_long, aes(x = Method, y = MSPE, fill = Method)) +
geom_boxplot(
width = 0.6,
alpha = 0.7,
outlier.shape = NA,
median.linewidth = 0   # removes median line
) +
stat_summary(
fun = mean,
geom = "crossbar",
width = 0.6,
color = "black",
linewidth = 1
) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
setwd("/Users/mclainfamily/Downloads/")
pdf("CV_analysis2.pdf", width = 9, height = 6)
ggplot(df_long, aes(x = Method, y = MSPE, fill = Method)) +
geom_boxplot(
width = 0.6,
alpha = 0.7,
outlier.shape = NA,
median.linewidth = 0   # removes median line
) +
stat_summary(
fun = mean,
geom = "crossbar",
width = 0.6,
color = "black",
linewidth = 1
) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
dev.off()
pdf("CV_analysis2.pdf", width = 9, height = 6)
ggplot(df_long, aes(x = Method, y = MSPE, fill = Method)) +
geom_boxplot(
width = 0.6,
alpha = 0.7,
outlier.shape = NA,
flatten = 0   # removes median line
) +
stat_summary(
fun = mean,
geom = "crossbar",
width = 0.6,
color = "black",
linewidth = 1
) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
dev.off()
pdf("CV_analysis2.pdf", width = 9, height = 6)
ggplot(df_long, aes(x = Method, y = MSPE, fill = Method)) +
geom_boxplot(
width = 0.6,
alpha = 0.7,
outlier.shape = NA,
fatten = 0   # removes median line
) +
stat_summary(
fun = mean,
geom = "crossbar",
width = 0.6,
color = "black",
linewidth = 1
) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
dev.off()
pdf("CV_analysis2.pdf", width = 9, height = 6)
ggplot(df_long, aes(x = Method, y = MSPE, fill = Method)) +
geom_boxplot(
width = 0.6,
alpha = 0.7,
outlier.shape = NA
) +
stat_summary(
fun = mean,
geom = "crossbar",
width = 0.6,
color = "black",
linewidth = 1
) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
dev.off()
pdf("CV_analysis2.pdf", width = 9, height = 6)
ggplot(df_long, aes(x = Method, y = MSPE, fill = Method)) +
geom_boxplot(
width = 0.6,
alpha = 0.7,
outlier.shape = NA,
median.color = NA,
median.linewidth = 0   # removes median line
) +
stat_summary(
fun = mean,
geom = "crossbar",
width = 0.6,
color = "black",
linewidth = 1
) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
dev.off()
pdf("CV_analysis2.pdf", width = 11, height = 6)
ggplot(df_long, aes(x = Method, y = MSPE, fill = Method)) +
geom_boxplot(
width = 0.6,
alpha = 0.7,
outlier.shape = NA,
median.color = NA,
median.linewidth = 0   # removes median line
) +
stat_summary(
fun = mean,
geom = "crossbar",
width = 0.6,
color = "black",
linewidth = 1
) +
geom_jitter(width = 0.15, size = 2.5, alpha = 0.8, color = "black") +
scale_fill_manual(values = method_colors) +
theme_minimal(base_size = 18) +
labs(
title = "Cross-Validated Prediction Error",
x = "",
y = "MSPE"
) +
guides(fill = "none") +
theme(
axis.text.x = element_text(angle = 30, hjust = 1),
plot.title = element_text(face = "bold")
)
dev.off()
library(tidyverse)
library(rstudioapi)
this.dir <- dirname(rstudioapi::getSourceEditorContext()$path)
dirname()
?dirname
this.dir <- dirname(rstudioapi::getSourceEditorContext()$path)
setwd(this.dir)
this.dir
framingham <- read_csv("/Users/mclainfamily/Library/CloudStorage/OneDrive-UniversityofSouthCarolina/Teaching/Summer Workshop/INBRE-Bios-Summer-26/data/framingham.csv")
dim(framingham)
names(framingham)
str(framingham)
library(this.path)
?this.path
this.path()
# Chunk 1
#| echo: false
library(tidyverse)
library(this.path)
this.dir <- this.dir()
setwd(this.dir)
framingham <- read_csv('/Users/mclainfamily/Library/CloudStorage/OneDrive-UniversityofSouthCarolina/Teaching/Summer Workshop/INBRE-Bios-Summer-26/data/framingham.csv')
# Chunk 3
dim(framingham)     # rows and columns
names(framingham)   # variable names
# Chunk 4
head(framingham)    # first 6 rows
#| eval: false
view(framingham)    # Look at the data interactively
# Create a factor
severity <- factor(c("mild", "severe", "moderate", "mild", "severe"),
levels = c("mild", "moderate", "severe"),
labels = c("Mild", "Moderate", "Severe"))
severity
levels(severity)
table(severity)
# Chunk 1
#| echo: false
library(tidyverse)
# Chunk 2
nums <- c(1.5, 2.3, 7.1, 4.8)
chars <- c("control", "treatment", "control", "treatment")
logicals <- c(TRUE, FALSE, TRUE, TRUE)
# Chunk 3
m <- matrix(1:9, nrow = 3, ncol = 3)
m
# Chunk 4
my_list <- list(
name   = "Patient A",
values = c(1.2, 3.4, 5.6),
flag   = TRUE
)
my_list
my_list$name
my_list$values
# Chunk 5
# A small tibble created by hand
patient_data <- tibble(
id        = 1:5,
treatment = c("A", "B", "A", "B", "A"),
response  = c(12.1, 8.4, 14.3, 9.1, 11.7)
)
patient_data
# Chunk 6
x <- c(10, 20, 30, 40, 50)
x[1]        # first element (R is 1-indexed, not 0-indexed)
x[3]        # third element
x[c(1, 3)]  # first and third elements
x[2:4]      # elements 2 through 4
x[-1]       # everything except the first element
# Chunk 7
x[x > 25]           # elements greater than 25
x[c(TRUE, FALSE, TRUE, FALSE, TRUE)]  # explicit logical index
# Chunk 8
bp <- c(systolic = 120, diastolic = 80)
bp["systolic"]
bp[c("systolic", "diastolic")]
# Chunk 9
patient_data <- data.frame(
id        = 1:5,
treatment = c("A", "B", "A", "B", "A"),
response  = c(12.1, 8.4, 14.3, 9.1, 11.7)
)
patient_data[1, ]        # first row, all columns
patient_data[, 2]        # all rows, second column (returns a vector)
patient_data[1, 2]       # first row, second column (single value)
patient_data[1:3, ]      # rows 1 through 3
patient_data[, c(1, 3)]  # columns 1 and 3
# Chunk 10
patient_data[, "response"]
patient_data[patient_data$treatment == "A", ]  # rows where treatment is "A"
# Chunk 11
patient_data$response        # the response column as a vector
patient_data$treatment       # the treatment column as a vector
mean(patient_data$response)  # pass directly to a function
# Chunk 12
my_list <- list(name = "Patient A", values = c(1.2, 3.4, 5.6))
my_list$name
my_list$values[2]  # chain indexing: second element of the values vector
# Chunk 13
patient_data[["response"]]   # same result as patient_data$response
my_list[["values"]]          # same as my_list$values
# Useful when the column name is stored in a variable
col_name <- "response"
patient_data[[col_name]]     # $ does not work here; [[ ]] does
# Chunk 14
# Patients in treatment A with response above 12
patient_data[patient_data$treatment == "A" & patient_data$response > 12, ]
# Chunk 15
library(nycflights13)
flights
# Chunk 16
mean(flights$dep_delay, na.rm = TRUE)
sd(flights$dep_delay, na.rm = TRUE)
range(flights$air_time, na.rm = TRUE)
# Chunk 17
# How many unique carriers are there?
unique(flights$carrier)
# How many flights per carrier?
table(flights$carrier)
# Chunk 18
x <- c("Control Group", "Treatment Group A", "Treatment Group B")
str_to_lower(x)          # convert to lowercase
str_to_upper(x)          # convert to uppercase
str_length(x)            # number of characters
str_detect(x, "Control") # does the string contain "Control"?
str_replace(x, "Group", "Arm") # replace a pattern
# Chunk 19
# Keep only carriers whose code starts with "U"
table(str_detect(flights$carrier, "^U"))
# Chunk 20
# Create a logical variable: was the flight delayed on arrival?
table(flights$arr_delay > 0)
# Chunk 21
sum(flights$arr_delay > 0, na.rm = TRUE)
mean(flights$arr_delay > 0, na.rm = TRUE)*100
# Chunk 22
# Create a factor
severity <- factor(c("mild", "severe", "moderate", "mild", "severe"),
levels = c("mild", "moderate", "severe"))
severity
levels(severity)
table(severity)
# Chunk 23
flights$origin_fct = factor(flights$origin)
flights$origin_fct[1:5]
# Chunk 24
severity_reref <- relevel(severity, ref = "moderate")
levels(severity_reref)
# Chunk 25
severity_recoded <- fct_recode(severity,
"Grade 1" = "mild",
"Grade 2" = "moderate",
"Grade 3" = "severe"
)
levels(severity_recoded)
# Chunk 26
# Collapse moderate and severe into a single "high" level
fct_collapse(severity,
"low"  = "mild",
"high" = c("moderate", "severe")
)
# Chunk 28
# Flights that arrived more than 2 hours late
flights |>
filter(arr_delay > 120)
# Flights departing in January on United or American
flights |>
filter(month == 1 &
carrier %in% c("UA", "AA")
)
# Chunk 29
# Flights departing in January not on United or American
flights |>
filter(month == 1 &
!(carrier %in% c("UA", "AA"))
)
# Keep only a few columns that contain "time"
flights |>
select_if(contains("time"))
?select_if
# Keep only a few columns that contain "time"
flights |>
select(contains("time"))
month.abb
flights |>
mutate(
date = make_date(year = year, month = month, day = day)
) |>
count(date)
flights |>
mutate(
date = make_date(year = year, month = month, day = day)
) |>
count(date) |>
arrange(n)
?arrange
flights |>
mutate(
# Create a date variable
date = make_date(year = year, month = month, day = day)
) |>
# Calculate the number of flights per day.
count(date) |>
# Display in decreasing order by the number of flights.
arrange(desc(n))
